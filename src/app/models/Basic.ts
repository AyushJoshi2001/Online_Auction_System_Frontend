export interface ProductAddDetails{
  title: string;
  product_pic?: string;
  description?: string;
  uid?: number;
}

export interface UserData {
  name: string;
  email: string;
  password: string;
  mobile: string;
  address: string;
  userType: string;
}

